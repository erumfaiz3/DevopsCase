apiVersion: v1
kind: Service
metadata:
  name: makemytrip-service
spec:
  selector:
    app: makemytrip
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30090
  type: NodePort


